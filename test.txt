it('setIsOfcrNmbrValid(): when cbOfficer.value === null sets isOfcrNmbrValid = true', () => {
  // arrange: ensure cbOfficer exists and value is null
  (component as any).cbOfficer = { value: null };

  // pre-check (optional)
  component.isOfcrNmbrValid = false;

  // act
  (component as any).setIsOfcrNmbrValid();

  // assert
  expect(component.isOfcrNmbrValid).toBeTrue();
});

it('setIsOfcrNmbrValid(): when cbOfficer.value is non-null sets isOfcrNmbrValid = false', () => {
  // arrange: cbOfficer has a non-null value
  (component as any).cbOfficer = { value: 'OFF123' };

  // pre-check (optional)
  component.isOfcrNmbrValid = true;

  // act
  (component as any).setIsOfcrNmbrValid();

  // assert
  expect(component.isOfcrNmbrValid).toBeFalse();
});