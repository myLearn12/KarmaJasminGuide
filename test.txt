it('ValidateFormControls(): should return true when any field has error', () => {

  // mock form controls
  component.fcn_AcctNumb = {
    markAsTouched: () => {},
    hasError: (name: string) => name === 'required'
  } as any;

  component.fcn_EstAccrual = {
    markAsTouched: () => {},
    hasError: () => false
  } as any;

  // other controls not needed for this branch
  component.cbStatus = { value: null } as any;
  component.cbDivision = { value: null } as any;
  component.cbCycle = { value: null } as any;
  component.cbFormula = { value: null } as any;
  component.cbCategory = { value: null } as any;
  component.cblAltDivision = { value: null } as any;

  // util isStringValid mock
  spyOn(component['utils'], 'isStringValid').and.returnValue(false);

  // act
  const result = component.ValidateFormControls();

  // assert - IF branch is hit → return true
  expect(result).toBeTrue();
});