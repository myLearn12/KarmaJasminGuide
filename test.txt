it('getRegionComboBoxValue(): should set _regionComboBox.value based on matching regionId', () => {
  // Arrange
  component.regions = [
    { regionId: 10, regionDescription: 'North' },
    { regionId: 20, regionDescription: 'South' }
  ];
  component.autocom = { regId: 20 } as any;

  (component as any)._regionComboBox = { value: null };

  // Act
  component.getRegionComboBoxValue();

  // Assert
  expect((component as any)._regionComboBox.value).toEqual(['20']);
});


it('getControlIdComboBoxValue(): should set _controlIdComboBox.value based on matching controlId', () => {
  // Arrange
  component.controlIds = [
    { conId: 100, controlDesc: 'Control A' },
    { conId: 200, controlDesc: 'Control B' }
  ];
  component.autocom = { controlId: 100 } as any;

  (component as any)._controlIdComboBox = { value: null };

  // Act
  component.getControlIdComboBoxValue();

  // Assert
  expect((component as any)._controlIdComboBox.value).toEqual(['100']);
});