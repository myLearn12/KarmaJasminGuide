it('should return early when ValidateFormControls() is true', () => {
  spyOn(component, 'ValidateFormControls').and.returnValue(true);
  component.onSearchButtonClick();
  expect(store.dispatch).not.toHaveBeenCalled();
});