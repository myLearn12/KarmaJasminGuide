it('setIsCategoryCdValid: should set true when cbCategory.value includes 62 or 45', fakeAsync(() => {
  (component as any).cbCategory = { value: ['62', '45'] };

  (component as any).setIsCategoryCdValid();
  tick();

  expect(component.isCategoryCdValid).toBeTrue();
}));
