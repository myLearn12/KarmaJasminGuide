const auditSpy = spyOn<any>(component['auditLogService'], 'writeAuditLog').and.callFake(() => {});


(component as any).auditLogService = {
  writeAuditLog: jasmine.createSpy('writeAuditLog')
};
const auditSpy = (component as any).auditLogService.writeAuditLog;

const auditSpy = spyOn<any>(component['auditLogService'], 'writeAuditLog').and.callFake(() => {});