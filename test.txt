it('onConfirmClick(): should hide alert modal', () => {
  component._alertModal = { hide: jasmine.createSpy('hide') } as any;

  component.onConfirmClick();

  expect(component._alertModal.hide).toHaveBeenCalled();
});