it('LoadControlId(): complete -> builds controlIdOptionsValues and controlIdComboBoxData when controlIds returned', fakeAsync(() => {
  // Defensive appConfig setup (component may use either casing)
  (component as any).appConfigService = (component as any).appConfigService || {} as any;
  (component as any).appConfigService.getAllControlIdurl = '/api/controlIds';
  (component as any).appConfigService.getAllControlIdUrl = '/api/controlIds';

  // Prepare sample service response (matches structure used in your method)
  const controlIds = [
    { conId: 200, controlDesc: 'Control 200' },
    { conId: 201, controlDesc: 'Control 201' }
  ];

  // Spy getRequest to return the array (completes -> will call complete handler)
  spyOn(component.feeApiservice, 'getRequest').and.callFake((url: string) => {
    // ensure test called the expected URL at least once (optional)
    expect(String(url)).toContain('/api/controlIds');
    return of(controlIds);
  });

  // Ensure starting state is empty so we can observe changes
  (component as any).controlIdOptionsValues = [];
  component.controlIdComboBoxData = [];

  // If component uses store.dispatch in complete/error paths, spy it to avoid errors
  if (component['store']) {
    spyOn(component['store'], 'dispatch').and.callFake(() => {});
  }

  // Act: call the method that triggers subscription + complete
  (component as any).LoadControlId();
  tick(); // flush observable/subscribe handlers

  // Build expected options array exactly as the method constructs them:
  // label: `${controlId.conId} ${controlId.controlDesc}`, value: controlId.conId
  const expectedOptions = [
    { label: '200 Control 200', value: 200 },
    { label: '201 Control 201', value: 201 }
  ];

  // Assert: internal options values populated by the for-loop
  expect((component as any).controlIdOptionsValues).toEqual(expectedOptions);

  // Assert: controlIdComboBoxData contains a single entry with options array
  expect(component.controlIdComboBoxData).toEqual([{ options: expectedOptions }]);

  // Also assert that the array length is > 0 (extra defensive check)
  expect((component as any).controlIdOptionsValues.length).toBeGreaterThan(0);
}));