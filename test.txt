it('onSearchButtonClick() - other value & valid string should call GenerateSearchAcctNumTableRows', () => {
  spyOn(component, 'GenerateSearchAcctNumTableRows').and.callFake(() => {});
  spyOn(store, 'dispatch').and.callFake(() => {});
  component.ValidateFormControls = jasmine.createSpy().and.returnValue(false);

  component._searchByRadioButton = { value: '3' };
  component.selectedSearchText = 'VALTEXT';
  component.utils = { isStringValid: () => true };
  component.appConfigService = { getAllAccountExportUrl: '/api/accounts/' };

  component.onSearchButtonClick();

  const urlString = '/api/accounts/';
  const query = '1|' + encodeURIComponent('VALTEXT');

  expect(component.loading).toBeTrue();
  expect(store.dispatch).toHaveBeenCalledWith(new LOADINGACTION(true));
  expect(component.GenerateSearchAcctNumTableRows)
    .toHaveBeenCalledWith(urlString, query);
});