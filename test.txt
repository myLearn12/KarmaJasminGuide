it('OnDivisionChange: when value is null → sets divisionRequired and calls revShareDivMappingMessage', fakeAsync(() => {
  // Arrange
  (component as any).cbDivision = { value: null };
  spyOn(component as any, 'revShareDivMappingMessage');

  // Act
  (component as any).OnDivisionChange();
  tick();

  // Assert
  expect(component.divisionRequired).toBeTrue();
  expect((component as any).revShareDivMappingMessage).toHaveBeenCalled();
}));
