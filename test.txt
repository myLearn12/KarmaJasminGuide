it('onoffcrNumberChange: should set officerRequired=true when cbofficer.value=null and update selectedofficerNm', fakeAsync(() => {
  // Arrange
  (component as any).cbofficer = { value: null };
  (component as any).fcn_OffcrNumber = { value: '123' };
  component.officers = { '123': { officerNm: 'John Doe' } } as any;

  const spySet = spyOn(component as any, 'setIsofcrNmbrValid');

  // Act
  (component as any).onoffcrNumberChange();
  tick();

  // Assert
  expect(component.officerRequired).toBeTrue();
  expect(component.selectedofficerNm).toBe('John Doe');
  expect(spySet).toHaveBeenCalled();
}));
