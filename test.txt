it('InitialDataLoad(): error branch - calls displayServiceErrors and dispatches LOADINGACTION(false)', () => {
  (component as any).regionid = 999;
  (component as any).appConfigService = {
    getRegionByIdUrl: '/regionById/',
    getProcessControlConfigByType: '/procByType/'
  };

  // feeApiService stub: make at least one call error (forkJoin will error)
  const feeApiSpy = jasmine.createSpyObj('feeApiService', ['getRequest']);
  feeApiSpy.getRequest.and.callFake((url: string) => {
    if (url.includes('/regionById/')) {
      // make the region call error
      return throwError({ error: { statusMessage: 'REGION_ERR' } });
    }
    // other call (if any)
    return of({ processType: null });
  });
  (component as any).feeApiservice = feeApiSpy;

  // store + displayServiceErrors spies
  const storeSpy = jasmine.createSpyObj('store', ['dispatch']);
  (component as any