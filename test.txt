it('getDivisionComboBoxValue(): should set divisionComboBox.value based on matching altDivisionCd', () => {
  // Arrange
  component.divisions = [
    { divisionId: 10, divisionCd: 'D1', divisionDesc: 'Division A' },
    { divisionId: 20, divisionCd: 'D2', divisionDesc: 'Division B' }
  ] as any;

  component.altdivabacostcntrmap = { altDivisionCd: 'D2' } as any;

  // mock combo box
  (component as any).divisionComboBox = { value: null };

  // Act
  (component as any).getDivisionComboBoxValue();

  // Assert
  expect((component as any).divisionComboBox.value).toEqual(['20']);
});