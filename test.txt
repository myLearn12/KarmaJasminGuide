it('InitialExcelDataLoad() – error path should call displayServiceErrors, clear loading, dispatch false', () => {
  component._searchByRadioButton = { value: '1' };
  component.selectedSearchText = 'abc';
  component.utils = { isStringValid: () => true };

  const errorObj = { error: { statusMessage: 'server failed' } };

  spyOn(component.subscription, 'add').and.callFake(() => {});
  spyOn(component.feeApiservice, 'getRequest').and.returnValue(throwError(() => errorObj));
  spyOn(component, 'displayServiceErrors');
  const dispatchSpy = spyOn((component as any).store, 'dispatch');

  component.InitialExcelDataLoad();

  expect(component.displayServiceErrors).toHaveBeenCalledWith('server failed');
  expect(component.loading).toBeFalse();
  expect(dispatchSpy).toHaveBeenCalledWith(new LOADINGACTION(false));
});